# See https://docs.github.com/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# Compiled output
/release
# Cualquier otra salida de electron-builder: el instalador interno
# (`release-internal`) y las carpetas alternas que hay que usar cuando Windows
# deja un `app.asar` bloqueado y no se puede escribir encima. Va por patron y no
# por nombre porque la primera variante que aparezca -y aparecen- se colaria
# entera en el repositorio: son 560 archivos y casi un giga.
/release-*
/dist
/out
/tmp
/out-tsc
/bazel-out
*.blockmap
latest.yml

# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.history/*

# Miscellaneous
/.angular/cache
.sass-cache/
/connect.lock
/coverage
/libpeerconnection.log
testem.log
/typings
docs/evidencias/

# El wasm del runtime de Rive. Lo copia `scripts/assets/rive.mjs` desde
# node_modules en cada build: son 849 KB de binario que ya estan ahi, y
# guardarlos tambien aqui obligaria a acordarse de actualizarlos a mano cada
# vez que suba la version del runtime.
/src/assets/rive/

# System files
.DS_Store
Thumbs.db

# De installer/ solo se versiona lo que es FUENTE. Fuera quedan el binario de
# SQL Express (314 MB, dependencia externa) y template.bak, que es un artefacto
# derivado: se genera con `npm run db:prepare-release` a partir del arbol de
# Git, nunca al reves.
/installer/*
!/installer/ConfigurationFile.ini
!/installer/setup-sqlserver.ps1
!/installer/preparar-red.ps1
!/installer/LEEME.md
!/installer/sql-servicing.json

.env

# Salida de Playwright: informes, trazas y capturas de una ejecucion concreta.
# No describen el producto, pesan y cambian en cada corrida.
/test-results
/playwright-report
/blob-report
